# PROJECT_CONTEXT

> Copy this file to the ROOT of your Expo project as `PROJECT_CONTEXT.md` and fill it in.
> The `expo-parity-engineer` skill reads it at the start of every task so the agent understands
> your app. Keep it short and factual. Delete sections that don't apply.

## App
- Name:
- What it does (1-3 lines):
- Target users:
- Languages / RTL support:

## Stack
- Expo SDK version:
- Language: TypeScript / JavaScript
- Navigation:
- State / data fetching:
- Backend (e.g. Supabase, Firebase, custom API):
- Key libraries (audio, images, animation, forms ...):

## Structure and conventions
- Source folder layout (paste `tree src -L 2` or describe):
- Theme / design tokens live in:
- Strings / i18n live in:
- Naming and code style rules specific to this project:
- Design reference files (e.g. `design.md`, `plan.md`):

## Environments and how to run
- Web preview: how it is started and where it is viewed:
- Phone (Expo Go via QR code): device, OS, Expo Go version:
- Env vars used (names only, never values):
- Any required setup steps:

## Known parity issues (web preview vs phone)
- Things that already look or behave differently, and why (if known):

## Known bugs / in progress
- 

## Rules for the agent in THIS project
- Do:
- Don't:
